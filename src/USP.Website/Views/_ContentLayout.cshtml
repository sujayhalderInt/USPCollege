@using USP.Business.Models.ContentModels
@inherits UmbracoViewPage
@{
    Layout = "~/Views/_MainLayout.cshtml";
}

@if (Model is IBaseContentBanner)
{
    @Html.Partial("HeroBanner", Model)
}

@RenderSection("ContentBelowBanner", required: false)

<div class="main-wrapper mt-50">
    @RenderBody()
</div>
